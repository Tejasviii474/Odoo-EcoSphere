/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Optional: Add domains for external images if needed later
  // images: {
  //   domains: ['example.com'],
  // },
};

module.exports = nextConfig;
